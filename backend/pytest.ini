[pytest]
# Only run tests in this directory; don't descend into venv/ (which has
# thousands of test files from installed packages that pytest would
# otherwise try to collect).
testpaths = .
norecursedirs = venv .venv env __pycache__ .git
